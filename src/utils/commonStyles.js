import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    backView: {
      marginTop: 20
    },
    cardTitle: {
      marginLeft: '10%',
      color: 'grey'
    }
  })

  export default styles